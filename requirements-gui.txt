# ⚠️ 遗留分支 —— 这不是交付目标，新环境不要用这个文件。
#
# 2026-09-16 定案：GUI 绑定**收敛到 PySide2 / Qt 5.15**（见 requirements-win7-gui.txt）。
#   原因：交付目标是 Win7，而 Qt 6 不支持 Win7。
#   开发与交付现在都用同一个 Python 3.8 环境，本文件只作历史保留。
#
# 如果哪天确定不再需要 Win7（或将来把交付面整体上移），再考虑启用 Qt 6。
#
# 许可证：Fluent 安装包标注 GPLv3；GPLv3 本身允许商用。
# 作者另有商业授权说明，详见 licenses/THIRD_PARTY_NOTICES.md。
#
# ⚠️ 不要与 PyQt-Fluent-Widgets / PyQt6-… / PySide2-… 同时安装，
#    它们的模块名都是 qfluentwidgets，会互相覆盖。
#
# 安装（仅在你确实要试 Qt 6 时）：
#   pip install -r requirements-gui.txt

PySide6-Fluent-Widgets>=1.11.3
